// Dotted filename: specifier './data-type-templates.service' must resolve to
// data-type-templates.service.ts (".service" is part of the name, not an extension).
import { dataTypeTemplatesService } from "./data-type-templates.service";

export const store = { service: dataTypeTemplatesService };
